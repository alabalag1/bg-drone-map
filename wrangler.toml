name = "bg-drone-map"
compatibility_date = "2024-11-01"

# Static-assets-only Worker: no Worker script (`main`), just the built site.
# `npx wrangler deploy` uploads everything under ./public and serves it.
[assets]
directory = "./public"
